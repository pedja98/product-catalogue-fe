import { Grid } from '@mui/material'
import { Outlet } from 'react-router-dom'

const TariffPlansPage = () => {
  return (
    <Grid>
      <Outlet />
    </Grid>
  )
}

export default TariffPlansPage
